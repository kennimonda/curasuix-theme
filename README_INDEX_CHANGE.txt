I replaced templates/index.liquid with a JSON template (index.json) for Online Store 2.0 compatibility. The JSON template lists the homepage sections in order so Shopify can render them as editable sections in the theme editor.

If you prefer a Liquid template instead, or want different section order/settings, edit templates/index.json or the section files in sections/.
